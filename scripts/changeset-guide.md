# Changeset 版本管理指南

## 🎯 版本级别说明

### Patch (补丁版本)
**格式**: `0.0.1` → `0.0.2`
**适用场景**:
- Bug 修复
- 小的样式调整
- 性能优化
- 文档更新
- 内部代码重构（不影响 API）

### Minor (次版本)
**格式**: `0.0.1` → `0.1.0`
**适用场景**:
- 新增功能（向后兼容）
- 新增组件
- 新增属性或方法
- 功能增强

### Major (主版本)
**格式**: `0.0.1` → `1.0.0`
**适用场景**:
- 破坏性变更（Breaking Changes）
- API 重构
- 移除废弃功能
- 架构重大调整

## 📝 创建 Changeset 的方法

### 方法一：交互式命令（推荐）

```bash
pnpm changeset add
```

这会启动交互式界面：
1. 选择受影响的包
2. 选择版本级别
3. 输入变更描述

### 方法二：手动创建文件

在 `.changeset/` 目录下创建 `.md` 文件：

#### Patch 示例
```markdown
---
"@seven-design-ui/components": patch
---

修复了 Button 组件的点击事件问题。
```

#### Minor 示例
```markdown
---
"@seven-design-ui/components": minor
---

新增了 Pagination 分页组件。
```

#### Major 示例
```markdown
---
"@seven-design-ui/components": major
"@seven-design-ui/core": major
---

重构了组件 API，提供了更好的 TypeScript 支持。
```

## 🔍 判断版本级别的决策树

### 1. 是否有破坏性变更？
- **是** → Major
- **否** → 继续下一步

### 2. 是否新增功能或组件？
- **是** → Minor
- **否** → 继续下一步

### 3. 是否修复 bug 或小改进？
- **是** → Patch

## 📋 实际使用示例

### 修复样式问题
```markdown
---
"@seven-design-ui/components": patch
---

修复了 Input 组件在暗色模式下的边框颜色问题。
```

### 新增组件
```markdown
---
"@seven-design-ui/components": minor
---

新增了 Cascader 级联选择组件，支持多级数据展示。
```

### API 重构
```markdown
---
"@seven-design-ui/components": major
---

重构了所有组件的 className 属性命名，统一使用 kebab-case 格式。
```

### 多包更新
```markdown
---
"@seven-design-ui/components": minor
"@seven-design-ui/core": patch
---

新增了 useControllableState Hook，并修复了核心工具函数的类型定义。
```

## ⚡ 快捷命令

```bash
# 查看当前待发布的变更
pnpm changeset status

# 预览版本更新结果（不实际修改）
pnpm changeset version --dry-run

# 手动更新版本号
pnpm changeset version

# 发布到 npm
pnpm changeset publish
```

## 🔧 注意事项

1. **包名必须用引号**: `"@seven-design-ui/components"`
2. **文件名任意**: 建议使用描述性名称，如 `fix-button-styles.md`
3. **多个包**: 可以在同一个文件中指定多个包的不同版本级别
4. **空变更**: 如果只是文档或配置更新，使用 `--empty` 标志

## 🎯 最佳实践

1. **及时创建**: 开发过程中及时创建 changeset，避免遗漏
2. **详细描述**: 写清楚变更内容，便于生成 changelog
3. **合理分级**: 准确选择版本级别，避免过度升级
4. **测试验证**: 发布前确保所有变更都经过测试

这样可以确保你的组件库版本管理规范且易于理解！🚀